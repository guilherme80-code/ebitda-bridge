/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';


/**
 * Returns server health status
 * @summary Health check
 */
export const HealthCheckResponse = zod.object({
  "status": zod.string()
})


/**
 * Returns all versions (BUDGET, MRF1..MRF7) with a flag indicating whether imported data exists, plus the default comparison pair. Any two versions with data can be compared; the bridge is computed on the fly.
 * @summary List available scenarios (version + period)
 */
export const ListScenariosResponse = zod.object({
  "scenarios": zod.array(zod.object({
  "id": zod.string().describe('Stable identifier (e.g. fy26_budget).'),
  "version": zod.string().describe('Version name (e.g. Budget, MRF7).'),
  "period": zod.string().describe('Period (e.g. FY26, FY26 Q1, FY26 Jan).'),
  "periodKind": zod.enum(['year', 'quarter', 'month']).describe('Granularity of the period. Only scenarios of the same kind can be compared (year vs year, quarter vs quarter, month vs month).'),
  "label": zod.string().describe('Display label in Portuguese.'),
  "hasData": zod.boolean().describe('Whether imported data exists for this version. Versions without data cannot be compared yet.')
}).describe('A scenario is a combination of version (e.g. Budget, MRF7) and period (e.g. FY26).')),
  "defaultPair": zod.object({
  "sourceId": zod.string(),
  "targetId": zod.string()
}).describe('A source\/target combination of scenarios.')
})


/**
 * Returns the ordered waterfall steps of the EBITDA bridge for a source/target scenario pair, including start/end totals and cumulative values.
 * @summary Get the consolidated EBITDA bridge
 */
export const GetBridgeQueryParams = zod.object({
  "source": zod.coerce.string().optional().describe('Source scenario id (version + period of origin). Defaults to the first version with imported data.'),
  "target": zod.coerce.string().optional().describe('Target scenario id (version + period of destination). Defaults to the most recent version with imported data.')
})

export const GetBridgeResponse = zod.object({
  "title": zod.string(),
  "unit": zod.string().describe('Unit of the values (MUSD).'),
  "steps": zod.array(zod.object({
  "key": zod.string().describe('Stable identifier of the component (e.g. vol_mix, selling_price).'),
  "label": zod.string().describe('Display label in Portuguese.'),
  "value": zod.number().describe('Impact in MUSD (positive or negative). For totals, the absolute level.'),
  "cumulative": zod.number().describe('Cumulative EBITDA level after this step, in MUSD.'),
  "kind": zod.enum(['total_start', 'delta', 'total_end']),
  "hasDetail": zod.boolean().describe('Whether a drill-down detail exists for this component.')
}).describe('One bar of the waterfall chart, in MUSD.'))
})


/**
 * Returns the supporting detail lines (from the Cálculo sheet) for one bridge component.
 * @summary Get drill-down details for a bridge component
 */
export const GetBridgeComponentParams = zod.object({
  "key": zod.coerce.string()
})

export const GetBridgeComponentQueryParams = zod.object({
  "source": zod.coerce.string().optional().describe('Source scenario id (version + period of origin). Defaults to the first version with imported data.'),
  "target": zod.coerce.string().optional().describe('Target scenario id (version + period of destination). Defaults to the most recent version with imported data.')
})

export const GetBridgeComponentResponse = zod.object({
  "key": zod.string(),
  "label": zod.string(),
  "value": zod.number().describe('Total impact of the component in MUSD.'),
  "unit": zod.string(),
  "lines": zod.array(zod.object({
  "id": zod.number(),
  "label": zod.string(),
  "group": zod.string().nullable().describe('Optional sub-grouping inside the component (e.g. Volume vs Mix, product family).'),
  "value": zod.number().describe('Impact in MUSD.'),
  "sortOrder": zod.number()
}).describe('One supporting line of a bridge component drill-down.'))
})


/**
 * Returns the detailed tables per driver (sales by product, fixed cost by category, input prices by item, usage, forex composition, stock/others), mirroring the Excel workbook driver tabs.
 * @summary Get detailed tables per bridge driver
 */
export const GetBridgeTablesQueryParams = zod.object({
  "source": zod.coerce.string().optional().describe('Source scenario id (version + period of origin). Defaults to the first version with imported data.'),
  "target": zod.coerce.string().optional().describe('Target scenario id (version + period of destination). Defaults to the most recent version with imported data.')
})

export const GetBridgeTablesResponse = zod.object({
  "title": zod.string(),
  "unit": zod.string(),
  "tables": zod.array(zod.object({
  "key": zod.string(),
  "title": zod.string(),
  "columns": zod.array(zod.object({
  "key": zod.string(),
  "label": zod.string()
}).describe('One numeric column of a detailed table; the label includes the unit.')),
  "rows": zod.array(zod.object({
  "label": zod.string(),
  "kind": zod.enum(['row', 'subtotal', 'total']),
  "values": zod.array(zod.number().nullable())
}).describe('One row of a detailed table. Values align with the columns array; null means not applicable.'))
}).describe('A detailed table for one bridge driver (mirrors an Excel driver tab).'))
})


/**
 * Interprets a Portuguese natural-language prompt (e.g. "se a venda de Calvert no MRF7 for 10% maior") into structured adjustments, applies them to the raw data of the selected scenario pair and recomputes the bridge.
 * @summary Simulate a what-if scenario from a natural-language prompt
 */
export const SimulateBridgeQueryParams = zod.object({
  "source": zod.coerce.string().optional().describe('Source scenario id (version + period of origin). Defaults to the first version with imported data.'),
  "target": zod.coerce.string().optional().describe('Target scenario id (version + period of destination). Defaults to the most recent version with imported data.')
})

export const SimulateBridgeBody = zod.object({
  "prompt": zod.string().describe('Natural-language what-if instruction in Portuguese.')
})

export const SimulateBridgeResponse = zod.object({
  "title": zod.string(),
  "unit": zod.string(),
  "interpretation": zod.string().describe('Human-readable summary of how the prompt was interpreted.'),
  "adjustments": zod.array(zod.string()).describe('Descriptions of the adjustments actually applied.'),
  "steps": zod.array(zod.object({
  "key": zod.string().describe('Stable identifier of the component (e.g. vol_mix, selling_price).'),
  "label": zod.string().describe('Display label in Portuguese.'),
  "value": zod.number().describe('Impact in MUSD (positive or negative). For totals, the absolute level.'),
  "cumulative": zod.number().describe('Cumulative EBITDA level after this step, in MUSD.'),
  "kind": zod.enum(['total_start', 'delta', 'total_end']),
  "hasDetail": zod.boolean().describe('Whether a drill-down detail exists for this component.')
}).describe('One bar of the waterfall chart, in MUSD.')).describe('Simulated waterfall steps (same shape as the base bridge).'),
  "deltaEbitda": zod.number().describe('Difference between the simulated and the original final EBITDA, in MUSD.'),
  "tables": zod.array(zod.object({
  "key": zod.string(),
  "title": zod.string(),
  "columns": zod.array(zod.object({
  "key": zod.string(),
  "label": zod.string()
}).describe('One numeric column of a detailed table; the label includes the unit.')),
  "rows": zod.array(zod.object({
  "label": zod.string(),
  "kind": zod.enum(['row', 'subtotal', 'total']),
  "values": zod.array(zod.number().nullable()).describe('Simulated values, aligned with the columns; null means not applicable.'),
  "baseValues": zod.array(zod.number().nullable()).describe('Original (pre-simulation) values, aligned with the columns.'),
  "changed": zod.array(zod.boolean()).describe('Whether each cell was affected by the simulation (aligned with the columns).')
}).describe('One row of a simulated detailed table, with the original values and change flags aligned to the columns.'))
}).describe('A detailed table recomputed with the simulated data, including original values per cell.')).describe('Detailed tables recomputed with the simulated data, with original values and change flags per cell.')
})


/**
 * Returns headline figures — start, end, total variation, largest positive and negative impacts.
 * @summary Get executive summary of the bridge
 */
export const GetBridgeSummaryQueryParams = zod.object({
  "source": zod.coerce.string().optional().describe('Source scenario id (version + period of origin). Defaults to the first version with imported data.'),
  "target": zod.coerce.string().optional().describe('Target scenario id (version + period of destination). Defaults to the most recent version with imported data.')
})

export const GetBridgeSummaryResponse = zod.object({
  "startLabel": zod.string(),
  "startValue": zod.number(),
  "endLabel": zod.string(),
  "endValue": zod.number(),
  "totalVariation": zod.number(),
  "largestPositive": zod.object({
  "key": zod.string(),
  "label": zod.string(),
  "value": zod.number()
}),
  "largestNegative": zod.object({
  "key": zod.string(),
  "label": zod.string(),
  "value": zod.number()
}),
  "positiveTotal": zod.number().describe('Sum of all positive impacts, MUSD.'),
  "negativeTotal": zod.number().describe('Sum of all negative impacts, MUSD.')
})


